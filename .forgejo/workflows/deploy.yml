name: Auto Pull on Main
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: docker
    steps:
      - name: Execute git pull on server
        run: |
          ssh -o StrictHostKeyChecking=no ralf@optimusprime.blackneon.net "cd /var/www/Nightboard83.de/app && git pull"
